<?xml version="1.0" encoding="utf-8"?>
<odoo>

    <!-- ═══ Partner Form — Add interaction fields ═══ -->
    <record id="view_partner_form_inherit_intake" model="ir.ui.view">
        <field name="name">res.partner.form.inherit.intake</field>
        <field name="model">res.partner</field>
        <field name="inherit_id" ref="base.view_partner_form"/>
        <field name="arch" type="xml">
            <div name="button_box" position="inside">
                <button class="oe_stat_button" icon="fa-phone"
                        type="object" name="action_view_interactions">
                    <field name="interaction_count" widget="statinfo" string="Interactions"/>
                </button>
            </div>
            <field name="category_id" position="after">
                <field name="customer_segment"/>
                <field name="subscription_status"/>
                <field name="lifetime_value"/>
            </field>
        </field>
    </record>

</odoo>
