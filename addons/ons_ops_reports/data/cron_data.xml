<?xml version="1.0" encoding="utf-8"?>
<odoo noupdate="1">

    <!-- ═══════════════════════════════════════════════════════════════
         Cron: Daily Report Recompute — runs at 05:00 UTC
         ═══════════════════════════════════════════════════════════════ -->
    <record id="cron_report_recompute_daily" model="ir.cron">
        <field name="name">Reports: Daily Recompute (Agent / Queue / Driver)</field>
        <field name="model_id" ref="model_ons_report_agent_daily"/>
        <field name="state">code</field>
        <field name="code">
env["ons.report.agent.daily"]._cron_recompute_daily()
env["ons.report.queue.daily"]._cron_recompute_daily()
env["ons.report.driver.daily"]._cron_recompute_daily()
        </field>
        <field name="interval_number">1</field>
        <field name="interval_type">days</field>
        <field name="nextcall">2026-04-03 05:00:00</field>
        <field name="active" eval="True"/>
    </record>

</odoo>
